# Frontend env example — copy to frontend/.env or .env.local and fill values
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: if backend runs on different host/port
VITE_BACKEND_URL=http://localhost:8001/api
